"use client";
import { Button } from "@/components/ui/button";
interface OpenSettingsPros {
  id: string;
}
const OpenSettings = ({ id }: OpenSettingsPros) => {
  return <Button>Open Settings</Button>;
};

export default OpenSettings;
